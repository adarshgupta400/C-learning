//working with numbers
  //number are of 2 types, 1-whole nmbers(int) ,2--decimals(float and doubles)
  //doubles allow to store more specific decimal places nubers. 
  #include <iostream>
 using namespace std;

 int main(void) {
    
	  cout<<-0.6588<<endl;
    cout<<5+2*7<<endl;  // allow +,-,*,/,%  ,and follow bodmass, to avoid bodmass need to use a parenthesis.
    cout<<(5+2)*7<<endl;

    int wnum=5; double dnum=5.5;
    wnum++; wnum*=2; cout<< wnum <<endl;

    //math between any 2 ints will always give int (5/3=1)
    //btw int and float or 2 floats will give always floats.
    

    return 0;
 }
